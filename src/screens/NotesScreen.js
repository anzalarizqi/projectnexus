// Phase 2/8 — Decoy Mode
// Shown when the decoy PIN is entered. A working minimal notes app.
// Stores real notes in an unencrypted table. Looks like a normal notes app.
export default function NotesScreen() {}
