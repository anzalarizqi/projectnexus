// Phase 9 — Movement Map
// MapLibre offline map. Pin dropping with preset labels. Opt-in location sharing (60s pulse).
//
// CUT FROM V1: march route drawing (hard UX under stress; pin dropping covers the real need).
// Available on Layer 1 and 2 only. Disabled with a clear message on Layer 3 (SMS).
export default function MapScreen() {}
