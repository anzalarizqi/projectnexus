// CUT FROM V1 — deferred to v1.1
// Requires stable mesh to show meaningful real-time data.
// Will be built once mesh is proven in the field and CSO partners
// give feedback on what Action Leaders actually want to see.
//
// Planned: pseudonym + role, current layer, last seen, press statement confirmation, crowd count.
export default function StatusBoardScreen() {}
