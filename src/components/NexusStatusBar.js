// Phase 3 — Persistent Status Bar
// Always visible at the top of every screen.
// Shows current active layer badge: INET (green) / BT + node count (blue) / SMS (amber) / UNREACHABLE (red).
// Props: layer ('INET'|'BT'|'SMS'|'UNREACHABLE'), nodeCount (number), smsBannerActive (bool)
export default function NexusStatusBar() {}
